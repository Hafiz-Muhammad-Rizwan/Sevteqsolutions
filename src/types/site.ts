export type NavItem = {
  label: string;
  href: string;
};

export type Capability = {
  id: string;
  title: string;
  description: string;
};

export type Industry = {
  name: string;
  summary: string;
};

export type Insight = {
  title: string;
  excerpt: string;
  category: string;
};

export type Metric = {
  value: string;
  label: string;
};
